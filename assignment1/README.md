## Notes on running JUnit Tests

Please ensure that `assignment1/` is the root directory before running JUnit tests,
otherwise `Assignment1Test.java` will not recognise the JUnit dependencies.

## JDK Versioning
JavaSE-25
